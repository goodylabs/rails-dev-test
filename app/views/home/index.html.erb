 <table class="table">
    <thead>
    <th scope="col">Product name</th>
    <th scope="col">Price [zł]</th>
    <th scope="col">Add</th>
    <th scope="col">Show</th>

    </tr>
    </thead>
    <tbody>
    <tr>
      <% @products.each do |product| %>
        <td><%= product.name %></td>
        <td><%= product.price %></td>
        <td>
        <% if user_signed_in? %>
        <%= form_with(url: cart_add_path) do |f| %>
        <%= f.hidden_field :id, value: product.id %>
        <%= f.number_field :quantity, value: 0, min: 1 %>
        <%= f.submit "Add to cart" %>
        <% end %>
                <% end %>

        </td>
                <td><%= link_to "show", product_show_path(product.id) %></td>

        <%
=begin%>
 <td><%= link_to  "Edit", edit_admin_product_path(product.id) %></td>
        <% if  @cart.include?(product) %>
          <td> <%= link_to image_tag('shopping_cart.png', alt: "Insert new", width: 24), show_cart_path, method: :delete %> </td>
        <% else %>
          <td> <%= link_to  "Add to cart", add_to_cart_path(product.id) %></td>
        <% end %> 
<%
=end%>


        </tr>

      <% end %>
      </tbody>
  </table>